package com.interview.shoppingcartpricing.domain.common;

/**
 * Represents supported currencies in the domain model.
 * More currencies can be added as the system evolves.
 */
public enum Currency {
    EUR
}
